/* 
A local Golf Club wants to run a raffel for its top ten members. 

They will draw 3 diffrent winners based on 3 diffrent catigories, name , id and age (one from each). 

Given the List of members "./members.js" split out the array of objects into three different arrays one for each property. 

the list of names should also be sorted alphabetically 
*/

//👉 Write the function your CodeWarriors will start with below here:
import members from "./members.js"

//dont change the function name !!!!!!! 

export default function split(members){

}
